from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Cluster Users based on their attributes observed and extracted from images, devices etc.',
    author='Amit Gupta (TrulyMadly)',
    license='',
)
